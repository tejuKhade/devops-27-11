pipeline {
    agent any
    stages {
    stage('pull-code') {
        steps {
            sh 'printenv'
        }
    }
    stage ('printenv') {
        steps {
            echo "build no is ${env.BUILD_ID}"
            echo "link to access this server ${env.GIT_BRANCH}"
}
}
    }
}