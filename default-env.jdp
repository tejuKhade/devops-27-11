pipeline {
    agent any
    stages {
    stage('pull-code') {
        steps {
            sh 'printenv'
        }
    }
    stage ('printenv') {
        steps {
            echo "build no is ${env.BUILD_ID}"
}
}